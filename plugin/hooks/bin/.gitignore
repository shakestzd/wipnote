# Build output — the system binary lives at ~/.local/bin/wipnote in dev mode.
# In dist mode, plugin/build.sh writes wipnote-bin (real binary) + wipnote
# (bootstrap wrapper) for marketplace distribution; those artifacts are also
# generated and should not be tracked.
wipnote
wipnote-dev
wipnote-bin
